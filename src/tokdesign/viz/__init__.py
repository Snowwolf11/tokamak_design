"""
tokdesign.viz
=============

Plotting utilities for tokdesign.

Design
------
• Plot modules are pure: they take arrays + metadata and return matplotlib figs/axes.
• Scripts (e.g. scripts/09_quicklook.py) handle I/O (HDF5 read, run_dir paths).
"""
